{#
/**
 * @file
 * Theme override to display a single page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template in this directory.
 *
 * Available variables:
 *
 * - page_type: type of page this is.
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration pages.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - messages: Status and error messages. Should be displayed prominently.
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Regions:
 * - page.highlighted: 'Highlighted: Status updates'
 * - page.site_header: 'Site Header: Branding'
 * - page.page_header: 'Page Header: Page Titles & Header Images'
 * - page.primary_menu: 'Primary Menu'
 * - page.breadcrumb: 'Breadcrumbs'
 * - page.local_tasks: 'Local Tasks'
 * - page.content_before: 'Content: Before'
 * - page.content_main: 'Content'
 * - page.content_after: 'Content: After'
 * - page.page_footer: 'Page Footer: CTA or other content'
 * - page.site_footer: 'Site Footer: Copyright, ADA, contact'
 *
 * @see template_preprocess_page()
 * @see html.html.twig
 */
#}
{% set classes = ["layout-container", "page--home"] %}
<div {{ attributes.addClass(classes) }}>

  {{ page.highlighted }}
  {{ page.site_header }}
  {{ page.primary_menu }}

  <a id="page-content" tabindex="-1"></a>
  {% include "@organisms/page/hero/hero.twig" with {
    title: page.title,
    suffix: page.suffix,
    hero_image: page.hero_image
  } %}

  <main>

    <div class="container">
      {{ page.breadcrumb }}
      {{ page.local_tasks }}
      {{ page.help }}
      {{ messages }}

      <a id="main-content" tabindex="-1"></a>
      <div class="layout-content">
        {{ page.content_before }}
      </div>{# /.layout-content #}
    </div>
    <div class="container-fluid background-highlight pl-0 pr-0">
      <div class="layout-content container">
        {{ page.content }}
      </div>{# /.layout-content #}
    </div>
    <div class="container">
      <div class="layout-content">
        {{ page.content_after }}
      </div>{# /.layout-content #}
    </div>

  </main>

  {{ page.page_footer }}
  {{ page.site_footer }}

</div>{# /.layout-container #}
